# Copy to .env.local to switch from mocks to the live FastAPI backend.
#
# Start the backend first:
#   cd ingen && uvicorn ingen.api.main:app --reload --port 8000
#
NEXT_PUBLIC_ADAPTER_MODE=http
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
